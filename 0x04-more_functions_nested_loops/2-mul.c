#include "main.h"

/**
 * mul - mulpiplies two integers
 * @a: takes the multiplicand
 * @b: takes the multiplier
 * Return: Always 0 (Success)
 */
int mul(int a, int b)
{
	int m;

	m = (a * b);
	return (0);
}
