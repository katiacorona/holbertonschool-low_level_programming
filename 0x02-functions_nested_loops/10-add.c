#include <stdio.h>
#include "main.h"

/**
 * add - Returns the result of adding two integers.
 *
 * @a: takes the first number to add.
 * @b: takes the second number to add.
 *
 * Return: The value of the sum.
 */
int add(int a, int b)
{
	int s;

	s = (a + b);
	return (s);
}
