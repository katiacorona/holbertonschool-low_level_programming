#include <stdio.h>
#include "main.h"

/**
 * _abs: Computes the absolute value of an int.
 *
 * Return: Always 0 (Success).
 */
int _abs(int)
{
	int i;
	
	for (i >= 0)
		return (i);
	
	else
		return (-i);
}
