#include <stdio.h>
#include "main.h"

/**
 * _abs: Computes the absolute value of an int.
 *
 * Return: The absolute of n.
 */
int _abs(int)
{
	int n;
	
	if (n >= 0)
		return (n);

	else
		return (-n);
}
